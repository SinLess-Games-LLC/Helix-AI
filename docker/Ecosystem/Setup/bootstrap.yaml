services:
  helix-bootstrap:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: sinless777/helix-bootstrap
    container_name: bootstrap
    hostname: bootstrap
    privileged: true
    secrets:
      - source: run.sh
        target: /app/run.sh
    command: /bin/bash /app/run.sh --tail
    environment:
      KEYSTORE_PASSWORD: lhsdghzvuawnlh8suvjli2360
    volumes:
      - ../../../.data/certs:/certs
      - ./config.yaml:/config/config.yaml
      - ./scripts:/app/scripts/
      - kafka_truststore:/kafka/certs
