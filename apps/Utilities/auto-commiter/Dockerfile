# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build auto-commiter`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t auto-commiter`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system auto-commiter && \
          adduser --system -G auto-commiter auto-commiter

COPY dist/apps/Utilities/auto-commiter auto-commiter/
RUN chown -R auto-commiter:auto-commiter .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix auto-commiter --omit=dev -f install

CMD [ "node", "auto-commiter" ]
