---
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: "{{ cloudflare.tunnel.domain }}"
  SECRET_ACME_EMAIL: "{{ cloudflare.tunnel.ssl.acme_email }}"
  SECRET_CLOUDFLARE_TUNNEL_ID: "{{ cloudflare.tunnel.id }}"
